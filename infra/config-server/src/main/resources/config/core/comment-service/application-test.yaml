spring:
  config:
    activate:
      on-profile: test
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:comment-test
    username: comment-test
    password: comment-test
  h2:
    console.enabled: true
  jpa:
    show-sql: TRUE
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true

hibernate:
  show_sql: TRUE

logging:
  ru:
    practicum: TRACE